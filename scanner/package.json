{
  "name": "kensara-deep-scanner",
  "version": "1.0.0",
  "private": true,
  "description": "Unified deep website scanner for the Kensara consent and gap-assessment tools.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check lib/scanner.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": { "playwright": "1.56.0" }
}
